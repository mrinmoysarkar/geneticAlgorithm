function new_pop = reproduction(pop)
m = size(pop,1);
n = size(pop,2);
old_pop = pop()
f = fit(pop);
f = f/sum(f);
f = cumsum(f);

new_pop = zeros(size(pop));
for i=1:m
    tem = find((rand<=f)==1);
    new_pop(i,:) = pop(tem(1),:);
end